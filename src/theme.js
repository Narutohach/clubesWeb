import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#006D31', // Cor principal
        },
        secondary: {
            main: '#506351', // Cor secundária
        },
    },
    // Adicione outras configurações de tema conforme necessário
});

export default theme;